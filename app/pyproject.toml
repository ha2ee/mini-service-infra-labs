[project]
name = "mini-service-infra-lab-app"
version = "0.1.0"
description = "Small FastAPI app for infrastructure and operations practice"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.135.1",
]
